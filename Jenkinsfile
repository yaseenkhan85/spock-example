pipeline {
    agent any
    stages {
        stage('Checkout') {
		steps{
            bat 'set'
		}
        }
        stage('Unit Test') {
		Steps{
	            bat 'set'
        	}
	}
        stage('Build') {
		Steps{
		bat 'set'
	    }
	}
    }
}
