pipeline {
    agent any
    stages {
        stage('Checkout') {
            bat 'set'
        }
        stage('Unit Test') {
	            bat 'set'
        }
        stage('Build') {
		bat 'set'
	    }
        stage('Deploy') {
		bat 'set'
	    }
        stage('Test') {
		bat 'set'
	    }
        
    }
}
